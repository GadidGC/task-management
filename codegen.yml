schema: https://syn-api-prod.herokuapp.com/graphql # point to your GraphQL server
generates:
  graphql/types.ts: # Where you want the generated files to go
    plugins:
      - typescript # For base TypeScript types
      - typescript-operations # For operation types (query/mutation/subscription)
